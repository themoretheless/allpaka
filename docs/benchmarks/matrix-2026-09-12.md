# Bench matrix — 2026-09-12 (M4 Max, MEGA off)

Artifacts: `.rbench/matrix-20260912-195149/`. Protocol: 3 fresh allpaka
processes then `llama-bench -r 3` (except 235B: one-shot pp128/tg8).
`ALLPAKA_BENCH_SKIP_MTP=1`. No `ALLPAKA_MEGA`.

## Unsupported on disk (skipped)

| Model | Why |
| --- | --- |
| `Qwen3.8-27B-UD-Q4_K_M` | arch `qwen35` dense — no registered capability (hybrid `qwen35moe` only) |
| `gemma-4-26B-A4B-it-UD-Q4_K_M` | arch `gemma4`, `head_dim=512` attention not implemented |

## Results (best / mean-ish warm)

| Model | Workload | llama.cpp | allpaka | ratio |
| --- | --- | ---: | ---: | ---: |
| qwen3-0.6b Q8_0 | rbench pp128 / tg32 | 8522 / 343 | 4364 / 389 | 0.51× / **1.13×** |
| qwen3-30b Q4_K_M | pp480 / tg32 | 1465 / 120 | **1500** / 115 | **1.02×** / 0.96× |
| Qwen3.6-35B Q4_K_M | pp480 / tg32 | 1402 / 80 | **1448** / **108** | **1.03×** / **1.35×** |
| GLM-4.5-Air Q4_K_M | pp480 / tg32 | 255±108 / **45.5** | **369** / 39.0 | **~1.4×** / **0.86×** |
| qwen3-235b Q2_K_XL | pp128 / tg8 | 124 / **27.8** | 123 / 20.0 | ~1.0× / **0.72×** |

Notes:

- Cool-machine session — absolute rates match historical 30B/35B/GLM bands.
- All listed allpaka runs: decode GPU path full successes, no declines.
- GLM llama prefill had huge variance (±107); decode gap is the stable signal.
- 235B decode is the largest remaining gap; short context, Q2_K path.
- Dense Qwen3.8 / Gemma need new kernels before they enter the matrix.

## Next model adds (when supported)

1. Dense Qwen (`qwen3` or future `qwen35` dense capability) mid-size.
2. Re-probe Gemma only after head_dim=512 attn.
3. Optional MTP-marked 35B for speculation parity, not rbench tok/s.
